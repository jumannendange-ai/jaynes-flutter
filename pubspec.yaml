name: jaynes_max_tv
description: JAYNES MAX TV - Tanzania Streaming App
publish_to: 'none'
version: 5.0.0+5

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Video Player - ExoPlayer based
  media_kit: ^1.1.10
  media_kit_video: ^1.2.4
  media_kit_libs_android_video: ^1.3.6

  # HTTP
  http: ^1.2.0
  dio: ^5.4.0

  # Storage
  shared_preferences: ^2.2.2

  # UI
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  flutter_staggered_grid_view: ^0.7.0

  # Firebase
  firebase_core: ^3.4.1
  firebase_messaging: ^14.9.4

  # Utils
  url_launcher: ^6.2.5
  connectivity_plus: ^5.0.2
  wakelock_plus: ^1.1.4
  flutter_native_splash: ^2.3.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
